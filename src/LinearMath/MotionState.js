(function( window, Bump ) {
  Bump.MotionState = Bump.type({
    members: {
      getWorldTransform: function() {
        Bump.Assert( false );
      },

      setWorldPosition: function() {
        Bump.Assert( false );
      }
    }
  });
})( this, this.Bump );
